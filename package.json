{
  "name": "planet2536-site",
  "private": true,
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "check:readiness": "node scripts/readiness.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  }
}
